#include <stdio.h>

int main(int argc, char* argv[]) {
	if (argc != 2) {
		printf("Error: the number of argments");
	}
	//[fsku] <input_file>

	//[file_name] [I/O command] [size]
	

	/* Stage1 - Initialization (empty input file) */

	/* Stage2 - Only w commands */

	/* Stage3 - Mix of w and r commands */

	/* Stage4 - Mix of w, r, and d commands */
	
	
	return 0;
}